import { BgInputColorDirective } from './bg-input-color.directive';

describe('BgInputColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BgInputColorDirective();
    expect(directive).toBeTruthy();
  });
});
